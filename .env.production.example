# Production build template — set these in Vercel / EAS before building.
# Never put secrets behind EXPO_PUBLIC_* (inlined into the client bundle).

EXPO_PUBLIC_API_URL=https://paisa-expense-tracker-backend.onrender.com/api/v1
