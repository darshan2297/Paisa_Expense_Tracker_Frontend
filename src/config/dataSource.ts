/**
 * Toggle mock vs live API data.
 *
 * Set to `false` once backend endpoints are ready — dashboard hooks will
 * switch from `src/mock/` to TanStack Query + API clients automatically.
 */
export const USE_MOCK_DATA = true;
